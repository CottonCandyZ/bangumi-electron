import { atom } from 'jotai'

export const mainContainerWidth = atom(0)

export const mainContainerLeft = atom(0)
export const mainContainerRight = atom(0)

export const mainContainerHeight = atom(0)
